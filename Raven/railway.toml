[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && pip install -e ."

[deploy]
startCommand = "python -m raven.api.main"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
